openapi: 3.2.0
info:
  title: Leonardo.AI Media API
  description: Upload, retrieve, and delete general-purpose media (images, video frames, reference assets) used across generation endpoints.
  version: v1.0.0
  contact:
    name: Leonardo.AI Support
    url: https://docs.leonardo.ai/docs/need-more-support
  license:
    name: Leonardo.AI Terms of Service
    url: https://leonardo.ai/terms-of-service/
servers:
- url: https://cloud.leonardo.ai/api/rest/v1
  description: Leonardo.AI Production API
security:
- bearerAuth: []
tags:
- name: Media
paths:
  /media:
    post:
      tags:
      - Media
      summary: Upload media
      description: This endpoint returns presigned details to upload a media file to S3
      operationId: uploadMedia
      requestBody:
        content:
          application/json:
            schema:
              properties:
                extension:
                  title: String
                  type: string
                  description: 'The file extension of the media file to upload. Supported extensions: mp4, mov.'
              required:
              - extension
              type: object
        description: Query parameters provided in the request body as a JSON object
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  uploadMedia:
                    properties:
                      uploadId:
                        title: String
                        type:
                        - string
                        - 'null'
                      url:
                        title: String
                        type:
                        - string
                        - 'null'
                      fields:
                        title: String
                        type:
                        - string
                        - 'null'
                    title: MediaUploadOutput
                    type:
                    - object
                    - 'null'
          description: Responses for POST /media
  /media/{id}:
    get:
      tags:
      - Media
      summary: Get uploaded media
      description: This endpoint returns details of an uploaded media record
      operationId: getUploadedMediaById
      parameters:
      - required: true
        description: _"id" is required_
        in: path
        name: id
        schema:
          pattern: '[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[89aAbB][a-f0-9]{3}-[a-f0-9]{12}'
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  uploaded_media_by_pk:
                    properties:
                      id:
                        title: uuid
                        type:
                        - string
                        - 'null'
                      createdAt:
                        title: timestamp
                        type:
                        - string
                        - 'null'
                      url:
                        title: String
                        type:
                        - string
                        - 'null'
                      thumbnailUrl:
                        title: String
                        type:
                        - string
                        - 'null'
                      status:
                        title: String
                        type:
                        - string
                        - 'null'
                      statusReason:
                        title: String
                        type:
                        - string
                        - 'null'
                      mediaType:
                        title: String
                        type:
                        - string
                        - 'null'
                      duration:
                        title: Float
                        type:
                        - number
                        - 'null'
                      width:
                        title: Int
                        type:
                        - integer
                        - 'null'
                      height:
                        title: Int
                        type:
                        - integer
                        - 'null'
                      video_fps:
                        title: Float
                        type:
                        - number
                        - 'null'
                    title: uploaded_media
                    type:
                    - object
                    - 'null'
          description: Responses for GET /media/{id}
    delete:
      tags:
      - Media
      summary: Delete uploaded media
      description: This endpoint deletes an uploaded media record and removes the associated file from S3
      operationId: deleteUploadedMediaById
      parameters:
      - required: true
        description: _"id" is required_
        in: path
        name: id
        schema:
          pattern: '[a-f0-9]{8}-[a-f0-9]{4}-4[a-f0-9]{3}-[89aAbB][a-f0-9]{3}-[a-f0-9]{12}'
          type: string
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                properties:
                  delete_uploaded_media_by_pk:
                    properties:
                      id:
                        title: uuid
                        type:
                        - string
                        - 'null'
                    title: uploaded_media
                    type:
                    - object
                    - 'null'
          description: Responses for DELETE /media/{id}
components:
  securitySchemes:
    bearerAuth:
      type: http
      bearerFormat: auth-scheme
      description: 'Bearer HTTP authentication. Allowed headers `Authorization: Bearer <api_key>`'
      scheme: bearer
